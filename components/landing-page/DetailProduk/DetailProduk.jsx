// import { useRouter } from "next/router";
// import { usePathname } from "next/navigation";
import { produk } from "@/constanst/katalog-produk";

const DetailProduk = () => {
  // const router = usePathname();
  // const { id } = router.query;

  // Cari produk berdasarkan ID dari router query
  // const selectedProduk = produk.find((item) => item.id == id);

  // Jika produk tidak ditemukan, tampilkan pesan bahwa produk tidak ada
  // if (!selectedProduk) {
  //   return <div>Produk tidak ditemukan</div>;
  // }
  return <div className="font-xl px-16 py-24">TEST</div>;
};

export default DetailProduk;
